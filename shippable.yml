language: java
jdk:
    - oraclejdk7
install:
    - sudo apt-get update
    - sudo apt-get install -u --fix-missing lib32stdc++6 lib32z1
    - ./gradlew clean assembleDebug
notifications:
    email:
        recipients:
            - x@xbc.nz
